{
  "name": "easyday",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm run --parallel dev",
    "start": "pnpm --filter platform dev",
    "web": "pnpm --filter platform web",
    "lint": "pnpm --filter platform typecheck"
  }
}
