# v1 stores this Week and optional Log, not a Goal

RunMax has no race Goal and no `race` Kind. The unauthenticated `demo` record is this Week plus optional source Log. Device-only storage would still split traces from the Board. Supersedes 0002.
