# Goal and Week live as one unauthenticated server record

The Board is Expo; traces need a Week id; MCP/RAG already require an agent server. Device-only storage would split state from traces. v1 has one implicit user and no auth — not a multi-tenant app.
