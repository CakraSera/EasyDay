# After Build, the runner may tap a card and change Kind

Read-only would keep `checkWeek` as the only writer. The runner still needs to swap Easy ↔ Walk without a full Rebuild. Kind edits are in v1. Whether an illegal tap (second Quality, zero Rest) is blocked is a follow-on decision.
