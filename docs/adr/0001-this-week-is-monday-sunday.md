# This Week is Monday–Sunday, not rolling 7 days

Sunday night plans the coming calendar week. `raceThisWeek` and evals need a stable `weekStart` (Monday). A rolling “next 7 days” from whenever they tap Build would move the race window and break fixtures.
