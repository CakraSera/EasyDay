# Server
PORT=8787
HOST=127.0.0.1

# Model provider (server-only; never VITE_*)
OPENAI_API_KEY=

# PostgreSQL (Prisma)
DATABASE_URL=postgresql://easyday:easyday@127.0.0.1:54329/easyday

# Embedded dev database (scripts/dev-db.ts) — local files only
DEV_DB_DIR=.devpg
DEV_DB_PORT=54329
DEV_DB_USER=easyday
DEV_DB_PASSWORD=easyday
DEV_DB_NAME=easyday
